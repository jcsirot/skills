{
  "skill_name": "code-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Review this API change as if it were a pull request. The handler builds a SQL query by concatenating the `userId` query parameter, returns records without checking tenant ownership, and has a test that only covers a valid ID. Identify actionable findings and state whether the change is ready to merge.",
      "expected_output": "The review starts from the pull request title and description when available, then identifies the injection risk and missing authorization, gives severity, location or affected code path, impact, a minimal fix, confidence, and merge readiness.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Review this small refactor that replaces a loop with a helper function. The behavior is covered by unit tests, the helper preserves error propagation, and no security or performance issue is apparent. Avoid inventing defects and report what validation remains.",
      "expected_output": "The review checks affected existing callers rather than only the helper diff, reports no actionable findings or only narrowly justified nits, explicitly avoids false positives, assesses whether unit coverage is sufficient, and distinguishes checks run from checks not run.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Review a background-job change that loads every pending row into memory, performs one database lookup per row, retries on timeout without an idempotency key, and leaves a timer running after shutdown. Prioritize the findings for a production merge decision.",
      "expected_output": "The review identifies the unbounded load, N+1 access, retry/idempotency risk, and resource-lifecycle issue with appropriate severity, impact, locations or affected paths, and minimal remediation.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Review this Java Spring service change. It catches `Exception` and returns an empty list, uses a JPA repository inside a loop for each order, accepts a polymorphic JSON payload, and starts a background executor without a shutdown path. Apply Java/Spring-specific review criteria rather than generic style advice.",
      "expected_output": "The review uses Java/Spring-specific reasoning, identifies broad exception masking, the JPA N+1 pattern, unsafe polymorphic deserialization if the configuration permits it, and executor lifecycle concerns with appropriate evidence and severity.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Review this Rust async change. It uses `unwrap()` on request-derived data, clones a large buffer before every send, holds a `MutexGuard` across an `.await`, and exposes an `unsafe` FFI wrapper without documenting or checking its safety invariants. Apply Rust-specific ownership, async, and unsafe-code criteria.",
      "expected_output": "The review uses Rust-specific reasoning, distinguishes panic risk from performance and async-lock issues, examines the unsafe FFI boundary and its invariants, and proposes minimal idiomatic fixes.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "Review this Kotlin coroutine change in an Android repository. A Flow is collected in a long-lived scope after the screen is destroyed, a Java platform value is force-unwrapped with `!!`, and a deep link launches an exported activity without validating the destination or caller. Apply Kotlin/Android-specific criteria.",
      "expected_output": "The review identifies lifecycle and structured-concurrency issues, the platform-nullability failure, and the exported deep-link authorization risk using Kotlin/Android-specific reasoning.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "Review this TypeScript web change with an HTML template and CSS module. It inserts a profile field with `innerHTML`, trusts a client-side TypeScript interface for request data, adds a `target=\"_blank\"` external link without opener protection, and uses a user-derived CSS value inside `url()`. Apply TypeScript, HTML, and CSS-specific security and quality criteria.",
      "expected_output": "The review separates compile-time type erasure, DOM XSS, external-link isolation, and CSS trust-boundary concerns, identifies the relevant markup or code locations, and avoids treating normal CSS style conventions as vulnerabilities.",
      "files": []
    }
  ]
}
